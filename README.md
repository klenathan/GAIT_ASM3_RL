This repository includes Jupyter notebooks and scripts that produce visualizations.

Note: Charts use Seaborn for improved aesthetics. Install dependencies from
`requirements.txt` (Seaborn has been added).
